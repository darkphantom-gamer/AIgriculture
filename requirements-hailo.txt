# AIgriculture — OPTIONAL Hailo-10H accelerator extras.
# Only needed if you own a Hailo AI HAT and run with --backend hailo.
#
# IMPORTANT: HailoRT / hailo_platform is NOT on PyPI. Install Hailo's SDK first
# from https://hailo.ai/developer-zone/ (the .deb / wheel for your HailoRT
# version and Python). This file lists only the extra pip packages on top of it.
#
# After installing the Hailo SDK, also install the base requirements:
#   pip install -r requirements.txt
#
# hailo_platform        # provided by the Hailo SDK install, not pip
# hailort               # provided by the Hailo SDK install, not pip
